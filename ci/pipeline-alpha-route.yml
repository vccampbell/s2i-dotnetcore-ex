---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: dotnetapp
  name: dotnetapp
  namespace: alpha
spec:
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
  to:
    kind: Service
    name: dotnetapp
    weight: 100
  wildcardPolicy: None
